


// this implements the moduolo operator, as opposed
// to the % operator, which is remainder.
// -1 % 3 = -1, but mod(-1, 3) = 2.
// this arithmetic wraps arround correctly,
// as corresponding to the division algorithm
int mod(int m, int n);
